spring:
  application:
    name: agentpay-core
  datasource:
    url: jdbc:postgresql://localhost:5432/core_db
    username: agentpay
    password: agentpay
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health
agentpay:
  chain-id: 31337
